spring.application.name=xyz
#spring.datasource.url=jdbc:mysql://localhost:3306/xyz?createTableIfNotExist=true
#spring.datasource.username=root
#spring.datasource.password=root
#spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# allow Railway to control port
server.port=${PORT:8080}

# database (use env vars Railway will provide)
spring.datasource.url=${JDBC_DATABASE_URL:${DB_URL}}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
